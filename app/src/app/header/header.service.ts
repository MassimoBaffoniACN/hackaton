import { Injectable } from '@angular/core';

// Nessuna logica dati per questo componente; file presente per contratto di struttura progetto.
@Injectable({ providedIn: 'root' })
export class HeaderService {}
